# Task: Package Details Page with Booking Confirmation

## Steps
- [x] 1. Create shared destinations data file `src/app/data/destinations.js` with rich package details
- [x] 2. Update homepage `src/app/page.js` to import shared data and link "Book Now" buttons to `/packages/[id]`
- [x] 3. Create dynamic package details page `src/app/packages/[id]/page.js` with details + booking confirmation form
- [x] 4. Verify build with `npm run build` (build succeeded, `/packages/[id]` route generated)
